#include <stdio.h>
#include <string.h>

struct paraQueue{
	int start=0;
	int end=0;
	int size = 10;
  char * queue[10];
  void enqueue(char * para);
  char * dequeue();
};

paraQueue::enqueue(char * para){
  queue[end]
}
